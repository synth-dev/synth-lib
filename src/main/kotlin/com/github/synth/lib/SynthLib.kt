package com.github.synth.lib

import net.minecraftforge.fml.common.*

@Mod(SynthLib.ModId)
object SynthLib {
    const val ModId: String = "synthlib"

    init {
        //...initialize mod here 
    }
}